// Projectile entity.
// Placeholder for the next step: the auto-firing weapon will create
// projectiles that fly toward enemies.

export class Projectile {
  constructor(x, y) {
    this.x = x;
    this.y = y;
  }

  update(deltaTime) {
    // Implemented in the next step.
  }
}
