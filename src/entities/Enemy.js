// Enemy entity.
// Placeholder for the next step: enemies will spawn around the player
// and walk toward them. Kept as an empty shell for now so the file
// structure is complete.

export class Enemy {
  constructor(x, y) {
    this.x = x;
    this.y = y;
  }

  update(deltaTime, player) {
    // Implemented in the next step.
  }
}
